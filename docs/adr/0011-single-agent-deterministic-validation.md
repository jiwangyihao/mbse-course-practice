---
Status: accepted
---

# 建模生成采用单 Agent 与确定性校验

建模生成流程采用一个建模 Agent 负责材料抽取、SysML v2 生成和修正建议，确定性校验器负责 schema、引用、一致性、覆盖关系和参数缺失检查。这个选择比多 Agent 流水线更易调试和交付，也比直接自由生成更可信；保留后续把校验器或评审器拆成独立 Agent 的空间，但首个课程项目不把多 Agent 编排作为核心风险。
