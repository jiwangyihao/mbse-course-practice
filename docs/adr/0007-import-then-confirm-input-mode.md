---
Status: accepted
---

# 采用材料导入加向导确认的模型输入方式

MBSE 建模工作台的输入方式采用“材料导入 + 向导确认”：用户导入或粘贴天问二号任务/需求材料，建模 Agent 抽取候选使命、约束、需求、分系统和关系，用户确认后再生成 SysML v2 文本与 JSON 视图模型。这个选择比纯自然语言导入更稳定，比纯结构化向导更能体现 Agent 价值，也避免预置模板削弱工具真实性；代价是需要设计抽取结果的确认界面和可回滚的生成流程。
